---
import { Image } from "astro:assets";

interface Props {
  src: ImageMetadata;
  alt: string;
}

const { src, alt } = Astro.props;
---

<div class="step-image-inline" data-alt={alt}>
  <Image src={src} alt={alt} loading="lazy" />
</div>
